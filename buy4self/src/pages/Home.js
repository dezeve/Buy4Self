import React from 'react';
import Layout from '../layout/Layout';

const Home = () => {
    return (
        <Layout>
            Products, Sidebar etc
        </Layout>
    );
};

export default Home;