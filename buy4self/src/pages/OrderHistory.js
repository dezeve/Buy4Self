import React from "react";
import Layout from "../layout/Layout";

const OrderHistory = () => {
    return (
        <Layout>
            <h1>Order History</h1>
        </Layout>
    );
}

export default OrderHistory;
